# ==============================================================================
#  MY PORTFOLIO - ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
#  Instructions:
#  1. Copy this file to .env.local:  cp .env.example .env.local
#  2. Update the values in .env.local with your actual secrets and config.
#  3. Never commit .env.local to version control.
# ==============================================================================

# ------------------------------------------------------------------------------
#  APPLICATION SETTINGS
# ------------------------------------------------------------------------------
# The public URL of your application (e.g., http://localhost:3000 or https://your-domain.com)
NEXT_PUBLIC_SERVER_URL="http://localhost:3000"

# Secret key for Payload CMS sessions and security.
# Generate a random string (e.g., using `openssl rand -base64 32`).
PAYLOAD_SECRET="YOUR_SECRET_KEY_HERE"

# ------------------------------------------------------------------------------
#  DATABASE CONNECTION (PostgreSQL)
# ------------------------------------------------------------------------------
# Connection string for the application to connect to the database.
# Format: postgres://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URI="postgres://payload:password@127.0.0.1:5432/payload"

# Variables used by Docker Compose to initialize the database container.
# Ensure these match the credentials in DATABASE_URI above if running locally with Docker.
POSTGRES_USER=payload
POSTGRES_PASSWORD=password
POSTGRES_DB=payload

# ------------------------------------------------------------------------------
#  PERSONAL INFORMATION (Displayed in Frontend)
# ------------------------------------------------------------------------------
# Used for Imprint, Contact sections, and SEO metadata.
NEXT_PUBLIC_FULL_NAME="John Doe"
NEXT_PUBLIC_EMAIL_ADDRESS="john.doe@example.com"
NEXT_PUBLIC_PHONE_NUMBER="+1 555 0123 456"
NEXT_PUBLIC_STREET_ADDRESS="123 Tech Avenue"
NEXT_PUBLIC_CITY_ADDRESS="San Francisco, CA 94107"

# ------------------------------------------------------------------------------
#  SOCIAL PROFILES
# ------------------------------------------------------------------------------
# Usernames only (not full URLs). Used to generate links dynamically.
NEXT_PUBLIC_GITHUB_USERNAME="johndoe"
NEXT_PUBLIC_LINKEDIN_USERNAME="johndoe"
NEXT_PUBLIC_INSTAGRAM_USERNAME="johndoe_grams"

# ------------------------------------------------------------------------------
#  EMAIL SERVICE (SMTP)
# ------------------------------------------------------------------------------
# Configuration for sending emails (e.g., contact form submissions).
SMTP_HOST="smtp.example.com"
SMTP_PORT=465
SMTP_USER="apikey"
SMTP_PASS="your-smtp-password"
